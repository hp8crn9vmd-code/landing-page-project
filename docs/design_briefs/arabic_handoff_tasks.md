# Arabic Handoff Tasks

Generated at: 2025-12-16T16:59:00.464394Z

The following native Arabic deliverables are required from the design team. Replace placeholders with final assets and update corresponding .meta.json files.

## product-screenshot.ar.with-frame.png

- **Deliverable**: Native Arabic UI screenshot (not mirrored). Provide both with-frame and no-frame variants where applicable.

- **Exact filenames required**: `product-screenshot.ar.with-frame.png`

- **Acceptance criteria**:

  1. Text is native Arabic (not mirrored English); use Arabic typography and correct diacritics where applicable.

  2. Layout is RTL and visually balanced; do not simply mirror English UI.

  3. File format: PNG with transparency; dimensions 1920x1200; max file size 204800 bytes.

  4. Provide @2x PNG fallback for retina where filename indicates @2x.

  5. Update `<filename>.meta.json` with actual_file_size_bytes, created_at, and set `requires_human_review: false` when ready.



## product-screenshot@2x.ar.png

- **Deliverable**: Native Arabic UI screenshot (not mirrored). Provide both with-frame and no-frame variants where applicable.

- **Exact filenames required**: `product-screenshot@2x.ar.png`

- **Acceptance criteria**:

  1. Text is native Arabic (not mirrored English); use Arabic typography and correct diacritics where applicable.

  2. Layout is RTL and visually balanced; do not simply mirror English UI.

  3. File format: PNG with transparency; dimensions 1920x1200; max file size 204800 bytes.

  4. Provide @2x PNG fallback for retina where filename indicates @2x.

  5. Update `<filename>.meta.json` with actual_file_size_bytes, created_at, and set `requires_human_review: false` when ready.



## product-screenshot.ar.no-frame.png

- **Deliverable**: Native Arabic UI screenshot (not mirrored). Provide both with-frame and no-frame variants where applicable.

- **Exact filenames required**: `product-screenshot.ar.no-frame.png`

- **Acceptance criteria**:

  1. Text is native Arabic (not mirrored English); use Arabic typography and correct diacritics where applicable.

  2. Layout is RTL and visually balanced; do not simply mirror English UI.

  3. File format: PNG with transparency; dimensions 1920x1200; max file size 204800 bytes.

  4. Provide @2x PNG fallback for retina where filename indicates @2x.

  5. Update `<filename>.meta.json` with actual_file_size_bytes, created_at, and set `requires_human_review: false` when ready.



## product-screenshot.ar.png

- **Deliverable**: Native Arabic UI screenshot (not mirrored). Provide both with-frame and no-frame variants where applicable.

- **Exact filenames required**: `product-screenshot.ar.png`

- **Acceptance criteria**:

  1. Text is native Arabic (not mirrored English); use Arabic typography and correct diacritics where applicable.

  2. Layout is RTL and visually balanced; do not simply mirror English UI.

  3. File format: PNG with transparency; dimensions 1920x1200; max file size 204800 bytes.

  4. Provide @2x PNG fallback for retina where filename indicates @2x.

  5. Update `<filename>.meta.json` with actual_file_size_bytes, created_at, and set `requires_human_review: false` when ready.


